class Mathexample {
    public static void main(String[] args) {
        double x = 14.7;
        double y = 25;

        // double z = Math.min(x, y);
        // double z = Math.max(x, y);
        // double z = Math.abs(y);
        // double z = Math.sqrt(y);
        // double z = Math.ceil(x);// roundoff
        double z = Math.floor(x);
        System.out.print(z);
    }
}
